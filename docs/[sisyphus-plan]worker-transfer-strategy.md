# 근로자 이직 및 다중 소속 전략 분석

> **생성일**: 2026-01-17
> **상태**: 전략 검토
> **우선순위**: 중간 (장기 확장성 대비)

---

## 1. 문제 정의

### 1.1 시나리오
```
A회사 근무 → 퇴사 → B회사 입사 (같은 전화번호, 둘 다 통패스 사용)
```

### 1.2 핵심 질문
1. 휴대폰 번호 UNIQUE 제약 하에서 어떻게 처리?
2. 이전 회사 이력을 어떻게 보존?
3. 다중 소속(파견 근로자) 지원 필요 여부?

---

## 2. 방안 비교 (장기 확장성 중심)

### 방안 1: 소프트 삭제 + 이력 테이블 ⭐ **[권장]**

#### 구조
```sql
-- 메인 테이블: 현재 소속만 (1:1)
CREATE TABLE users (
  id UUID PRIMARY KEY,
  phone VARCHAR(20) UNIQUE NOT NULL,
  company_id BIGINT NOT NULL,
  site_id BIGINT NOT NULL,
  partner_id BIGINT NOT NULL,
  status worker_status NOT NULL,
  ...
);

-- 이력 테이블: 과거 소속 (1:N)
CREATE TABLE user_employment_history (
  id BIGSERIAL PRIMARY KEY,
  user_id UUID NOT NULL REFERENCES users(id),
  company_id BIGINT NOT NULL,
  site_id BIGINT NOT NULL,
  partner_id BIGINT NOT NULL,
  role user_role NOT NULL,
  joined_at TIMESTAMPTZ NOT NULL,
  left_at TIMESTAMPTZ,
  leave_reason VARCHAR(50),  -- 'RESIGNED', 'TRANSFERRED', 'FIRED'
  created_at TIMESTAMPTZ DEFAULT NOW()
);
```

#### 장점
✅ **단순성**: 기존 코드 최소 수정
✅ **성능**: 메인 쿼리는 단일 테이블 조회
✅ **이력 보존**: 법적 요구사항 충족
✅ **휴대폰 UNIQUE 유지**: 보안 강화
✅ **점진적 확장**: 필요 시 이력 테이블만 확장

#### 단점
❌ 동시 다중 소속 불가 (파견 근로자)
❌ 이직 시 수동 처리 필요
❌ 이력 테이블 추가 관리

#### 확장성 점수: 7/10

**확장 시나리오:**
- **Case 1: 파견 근로자 지원** → 방안 2로 마이그레이션 필요
- **Case 2: 데이터 분석** → 이력 테이블로 충분
- **Case 3: SaaS 확장** → 추가 수정 없음

---

### 방안 2: 다중 소속 허용 (관계 테이블 분리) ⭐⭐ **[장기 최적]**

#### 구조
```sql
-- 개인 정보 테이블
CREATE TABLE persons (
  id UUID PRIMARY KEY,
  phone VARCHAR(20) UNIQUE NOT NULL,
  name VARCHAR(50) NOT NULL,
  birth_date DATE NOT NULL,
  gender CHAR(1),
  nationality VARCHAR(10),
  created_at TIMESTAMPTZ DEFAULT NOW()
);

-- 소속 관계 테이블
CREATE TABLE user_memberships (
  id BIGSERIAL PRIMARY KEY,
  person_id UUID NOT NULL REFERENCES persons(id),
  company_id BIGINT NOT NULL,
  site_id BIGINT NOT NULL,
  partner_id BIGINT NOT NULL,
  role user_role NOT NULL,
  status worker_status NOT NULL,

  -- 이력
  joined_at TIMESTAMPTZ NOT NULL,
  left_at TIMESTAMPTZ,
  is_current BOOLEAN DEFAULT true,

  -- 제약
  UNIQUE(person_id, company_id, site_id, partner_id, is_current),  -- 동일 소속 중복 방지
  CHECK (NOT (is_current = true AND left_at IS NOT NULL))  -- 현재 소속은 퇴사일 없음
);

-- 인증 정보 (Supabase Auth 연동)
CREATE TABLE auth_credentials (
  id UUID PRIMARY KEY REFERENCES persons(id),
  supabase_user_id UUID UNIQUE NOT NULL,
  last_login TIMESTAMPTZ
);
```

#### 장점
✅ **다중 소속 지원**: 파견 근로자, 겸직 가능
✅ **자동 이력 관리**: 별도 이력 테이블 불필요
✅ **데이터 정규화**: 개인정보 중복 제거
✅ **확장성 극대화**: 미래 요구사항 대응
✅ **분석 용이**: JOIN으로 모든 이력 조회

#### 단점
❌ **복잡도 증가**: 모든 쿼리에 JOIN 필요
❌ **기존 코드 전면 수정**: 3주+ 작업량
❌ **성능 우려**: JOIN 오버헤드
❌ **인증 복잡**: 다중 소속 시 컨텍스트 전환 필요

#### 확장성 점수: 10/10

**확장 시나리오:**
- **Case 1: 파견 근로자** → 즉시 지원
- **Case 2: 데이터 분석** → 완벽 지원
- **Case 3: SaaS 확장** → 추가 수정 없음
- **Case 4: B2B2C 확장** → person 테이블 재사용

---

### 방안 3: 복합키 사용 ❌ **[비권장]**

#### 구조
```sql
CREATE TABLE users (
  phone VARCHAR(20) NOT NULL,
  company_id BIGINT NOT NULL,
  name VARCHAR(50) NOT NULL,
  birth_date DATE NOT NULL,
  ...
  PRIMARY KEY (phone, company_id)
);
```

#### 장점
✅ 구현 초간단
✅ 이직 시 자동 처리

#### 단점
❌ **개인정보 중복**: 이름, 생년월일 여러 번 저장
❌ **이력 조회 복잡**: 여러 레코드 집계 필요
❌ **Supabase Auth 충돌**: user_id 불일치
❌ **데이터 무결성**: 정보 불일치 위험
❌ **스토리지 낭비**: 중복 데이터

#### 확장성 점수: 2/10

---

## 3. 장기 확장성 시나리오 분석

### 시나리오 1: 파견 근로자 지원 (확률: 70%)
```
근로자 A가 동시에:
- A회사 건설 현장 (주 3일)
- B회사 제조 공장 (주 2일)
```

| 방안 | 지원 가능 | 구현 난이도 |
|------|:---------:|:----------:|
| 방안 1 | ❌ → 방안 2로 마이그레이션 필요 | 상 |
| 방안 2 | ✅ 즉시 지원 | - |
| 방안 3 | △ 가능하나 컨텍스트 전환 복잡 | 상 |

---

### 시나리오 2: 데이터 분석 플랫폼 (확률: 80%)
```
요구사항:
- 근로자 평균 근속 기간
- 회사 간 이동 패턴
- 고위험 현장 분석
```

| 방안 | 쿼리 복잡도 | 성능 |
|------|:----------:|:----:|
| 방안 1 | 중간 (이력 테이블 JOIN) | 좋음 |
| 방안 2 | 낮음 (단일 테이블) | 매우 좋음 |
| 방안 3 | 높음 (복잡한 GROUP BY) | 나쁨 |

---

### 시나리오 3: SaaS 멀티테넌시 확장 (확률: 60%)
```
요구사항:
- 회사별 완전 격리
- 크로스 컴퍼니 데이터 차단
```

| 방안 | RLS 정책 | 데이터 격리 |
|------|:--------:|:----------:|
| 방안 1 | 간단 | 완벽 |
| 방안 2 | 복잡 (membership 테이블) | 완벽 |
| 방안 3 | 매우 복잡 | 위험 |

---

### 시나리오 4: 법적 요구사항 (확률: 90%)
```
근로기준법: 4대 보험, 퇴직금 계산을 위한 근로 이력 보존 의무
```

| 방안 | 이력 보존 | 법적 적합성 |
|------|:---------:|:----------:|
| 방안 1 | ✅ 완벽 | 상 |
| 방안 2 | ✅ 완벽 | 상 |
| 방안 3 | △ 조회 복잡 | 중 |

---

## 4. 비용 분석

### 개발 비용

| 항목 | 방안 1 | 방안 2 | 방안 3 |
|------|--------|--------|--------|
| **초기 개발** | 3일 | 3주 | 1일 |
| **테스트** | 2일 | 1주 | 1일 |
| **문서화** | 1일 | 3일 | 0.5일 |
| **총계** | **6일** | **4주** | **2.5일** |

### 운영 비용 (연간)

| 항목 | 방안 1 | 방안 2 | 방안 3 |
|------|:------:|:------:|:------:|
| **DB 스토리지** | +5% | +10% | +30% (중복) |
| **쿼리 비용** | 기준 | +20% (JOIN) | +50% (복잡 쿼리) |
| **유지보수** | 낮음 | 중간 | 높음 (버그) |

---

## 5. 최종 권장 사항

### 🎯 **단기 전략 (MVP ~ 6개월)**: 방안 1

**이유:**
1. **빠른 출시**: 6일 내 구현 가능
2. **안정성**: 검증된 패턴 (INACTIVE + 이력)
3. **비용 효율**: 개발 비용 최소화
4. **기존 코드 재사용**: admin-web 수정 최소

**적용 시점:** 즉시

---

### 🚀 **중장기 전략 (6개월 ~ 1년)**: 방안 2로 마이그레이션

**이유:**
1. **파견 근로자 지원**: 시장 확대 필수
2. **데이터 분석**: 경쟁 우위 확보
3. **확장성**: 미래 요구사항 대응

**마이그레이션 트리거:**
- 파견 근로자 요청 3건 이상
- 고객사 20개 돌파
- 데이터 분석 기능 출시 시

**마이그레이션 계획:**
```sql
-- Step 1: persons 테이블 생성
CREATE TABLE persons AS
SELECT DISTINCT
  id,
  phone,
  name,
  birth_date,
  gender,
  nationality,
  created_at
FROM users;

-- Step 2: user_memberships로 현재 데이터 이관
INSERT INTO user_memberships (person_id, company_id, site_id, partner_id, ...)
SELECT id, company_id, site_id, partner_id, ...
FROM users
WHERE status = 'ACTIVE';

-- Step 3: 이력 데이터 이관
INSERT INTO user_memberships (person_id, company_id, ..., is_current, left_at)
SELECT user_id, company_id, ..., false, left_at
FROM user_employment_history;

-- Step 4: users 테이블 뷰로 전환 (하위 호환)
CREATE VIEW users AS
SELECT
  p.id,
  p.phone,
  p.name,
  m.company_id,
  m.site_id,
  m.partner_id,
  m.role,
  m.status,
  ...
FROM persons p
JOIN user_memberships m ON p.id = m.person_id
WHERE m.is_current = true;
```

---

## 6. 단계별 로드맵

### Phase 1: 방안 1 구현 (현재 ~ 1주)
- [ ] `user_employment_history` 테이블 마이그레이션
- [ ] 퇴사 처리 로직 (웹)
- [ ] 이직 감지 로직 (앱)
- [ ] 이력 조회 UI (웹)

### Phase 2: 모니터링 (1주 ~ 6개월)
- [ ] 파견 근로자 요청 수 추적
- [ ] 이직 빈도 분석
- [ ] 고객 피드백 수집

### Phase 3: 방안 2 마이그레이션 결정 (6개월)
**조건 충족 시 실행:**
- 파견 근로자 요청 >= 3건/월
- 고객사 >= 20개
- 데이터 분석 기능 로드맵 확정

### Phase 4: 방안 2 구현 (마이그레이션 결정 후 4주)
- [ ] 스키마 설계 최종 확정
- [ ] 마이그레이션 스크립트 작성
- [ ] 하위 호환 뷰 생성
- [ ] 단계적 배포 (Blue-Green)

---

## 7. 리스크 관리

### 방안 1 리스크

| 리스크 | 확률 | 영향도 | 완화 방안 |
|--------|:----:|:------:|----------|
| 파견 근로자 요청 급증 | 중간 | 높음 | 방안 2 마이그레이션 계획 사전 수립 |
| 이력 데이터 누락 | 낮음 | 중간 | 트리거 자동화 + 모니터링 |
| 이직 처리 실패 | 중간 | 중간 | 트랜잭션 롤백 + 에러 알림 |

### 방안 2 리스크

| 리스크 | 확률 | 영향도 | 완화 방안 |
|--------|:----:|:------:|----------|
| 마이그레이션 실패 | 낮음 | 매우 높음 | 단계적 배포 + 롤백 계획 |
| 성능 저하 | 중간 | 중간 | 인덱스 최적화 + 캐싱 |
| 개발 지연 | 높음 | 중간 | 전담 팀 배정 |

---

## 8. 의사결정 매트릭스

| 기준 | 가중치 | 방안 1 | 방안 2 | 방안 3 |
|------|:------:|:------:|:------:|:------:|
| **개발 속도** | 30% | 9 | 3 | 10 |
| **장기 확장성** | 25% | 6 | 10 | 2 |
| **운영 안정성** | 20% | 8 | 7 | 4 |
| **비용 효율** | 15% | 9 | 6 | 8 |
| **법적 적합성** | 10% | 9 | 9 | 6 |
| **총점** | 100% | **7.95** | **6.55** | **6.1** |

### 점수 해석
- **방안 1**: 단기 최적 (빠른 출시)
- **방안 2**: 장기 최적 (확장성)
- **방안 3**: 비권장

---

## 9. 결론

### ✅ 최종 권장
1. **현재**: 방안 1 즉시 구현 (1주 내)
2. **6개월 후**: 시장 상황 평가
3. **필요 시**: 방안 2로 마이그레이션

### 핵심 메시지
> "완벽한 아키텍처보다 빠른 시장 출시가 우선입니다.
> 단, 확장 경로를 미리 설계하여 기술 부채를 최소화합니다."

---

## 10. 참고 문서
- `/docs/signin/통패스_근로자앱_가입_PRD.md`
- `/.sisyphus/plans/mobile-pc-integration-plan.md`
- `/CLAUDE.md` - 조직 체계 및 권한
- `/docs/DATABASE.md` - 스키마 설계

---

**작성자**: Claude (AI Assistant)
**검토 필요**: PM, Backend Lead, CTO
