# 최초 위험성 평가 만들기 - 구현 상세서

> 작성일: 2026-01-20
> 상태: 구현 중
> 담당: Claude Code

## 1. 개요

최초 위험성 평가를 작성하는 페이지입니다. 현장이 선택된 상태에서 대분류(작업공종) → 소분류 → 위험요인을 계층적으로 입력하여 위험성 평가를 만듭니다.

### 주요 특징
- 계층적 데이터 구조 (대분류 → 소분류 → 위험요인)
- Mock 데이터 기반 (SQLite DB 연동 전)
- 실시간 유효성 검사
- 위험도별 색상 코딩
- 중복 방지 기능

## 2. 페이지 정보

### 2.1 기본 정보
- **경로**: `/safety/risk/create/initial`
- **제목**: 최초 위험성 평가 만들기
- **레이아웃**: `<InitialAssessmentForm />` 컴포넌트

### 2.2 파일 구조
```
apps/admin-web/src/pages/risk-assessment/
├── CreateAssessmentPage.tsx           # 라우터 페이지
├── InitialAssessmentForm.tsx          # 메인 폼 컴포넌트
├── components/
│   ├── BasicInfoSection.tsx           # 기본 정보 섹션
│   ├── CategorySearchInput.tsx        # 대분류 검색 입력
│   ├── SubcategoryCheckList.tsx       # 소분류 체크리스트
│   ├── CategoryItem.tsx               # 대분류 아이템 (소분류 포함)
│   └── RiskFactorCard.tsx             # 위험요인 카드
└── modals/
    ├── SubcategoryAddModal.tsx        # 소분류 직접 추가 모달
    ├── RiskFactorSelectModal.tsx      # 위험요인 선택 모달
    └── SuccessModal.tsx               # 완료 모달
```

## 3. 데이터 구조

### 3.1 TypeScript 인터페이스

```typescript
interface RiskFactor {
  id: string;
  factor: string;                      // 위험요인
  level: 'HIGH' | 'MEDIUM' | 'LOW' | null; // 위험성 수준
  improvement: string;                 // 개선대책
  workPeriodStart: string;             // 작업 시작일
  workPeriodEnd: string;               // 작업 종료일
}

interface Subcategory {
  id: number;
  name: string;
  isCustom?: boolean;                  // 직접 추가 여부
  riskFactors: RiskFactor[];
}

interface Category {
  id: string;                          // temp-{timestamp}-{counter}
  categoryId: number | null;           // DB ID
  categoryName: string;
  subcategories: Subcategory[];
}
```

### 3.2 Mock 데이터

#### 소분류 Mock 데이터
```typescript
{
  101: '가설전선 설치작업',
  102: '가설전선 점검작업',
  103: '꽂음 접속기작업',
  104: '이동형 릴 전선작업',
  105: '전동공구 사용/정리정돈작업',
}
```

#### 위험요인 Mock 데이터
```typescript
[
  {
    id: 1,
    factor: '안전대를 사용하지 않고 고소부위 작업중 추락',
    improvement: '고소부위 작업시 안전대 고리 체결 철저',
  },
  {
    id: 2,
    factor: '작업 발판 미설치로 인한 추락',
    improvement: '안전 작업발판 설치 후 작업 실시',
  },
  // ... 추가 항목
]
```

## 4. 주요 기능

### 4.1 기본 정보 입력
- **현장명**: 고정값 (통사통사현장)
- **회사명**: 고정값 ((주)통하는사람들) - "시공사" 표시 제거됨
- **결재라인**: 선택 (미구현 - 추후 모달 연동)
- **근로자 대표**: 고정값 (36 / 0017)
- **작업기간**: 날짜 선택기

### 4.2 대분류(작업공종) 관리

#### 추가
- "작업 공종(대분류) 추가하기" 버튼 클릭
- 빈 대분류 카드 생성 (임시 ID: `temp-{timestamp}-{counter}`)

#### 선택
- `CategorySearchInput` 컴포넌트로 검색 (Mock)
- 선택 시 소분류 체크리스트 표시

#### 삭제
- 각 대분류 카드 우측 "대분류 삭제" 버튼

### 4.3 소분류 관리

#### 체크리스트 선택
- 대분류 선택 시 자동으로 관련 소분류 목록 표시
- 복수 선택 가능 (체크박스)
- 선택된 소분류만 하단에 표시

#### 직접 추가
- "소분류 직접 추가" 버튼 클릭
- `SubcategoryAddModal` 모달 표시
- 이름 입력 후 추가
- `isCustom: true` 플래그 설정
- 임의 ID 생성: `Math.floor(Math.random() * 1000000) + 1000`

#### 표시 형식
- **일반 소분류**: `소분류① 가설전선 설치작업`
- **직접 추가**: `소분류② 커스텀 소분류명 (직접 추가)`
- 순환 번호: ①②③④⑤⑥⑦⑧⑨⑩

#### 삭제
- 소분류 헤더 우측 X 버튼 클릭
- 해당 소분류와 포함된 모든 위험요인 삭제

### 4.4 위험요인 관리

#### 추가 방법

##### 1. 목록에서 선택
- "위험요인 추가하기" 버튼 클릭 (왼쪽 정렬)
- `RiskFactorSelectModal` 모달 열림
- **일반 소분류**: 목록에서 선택 모드
- **커스텀 소분류**: 직접 입력 모드 (추천 목록 없음 안내)

**목록 선택 특징:**
- 검색 기능 제공
- 이미 추가된 위험요인 자동 제외 (중복 방지)
- 체크박스로 복수 선택 가능

##### 2. 직접 입력
- 모달에서 "직접 입력" 탭 선택
- 위험요인 + 개선대책 textarea 입력
- 중복 체크: 이미 추가된 위험요인과 같은 이름 입력 시 alert

#### 위험요인 카드 구성
```
┌─────────────────────────────────────────┐
│ [항목 삭제]                              │
│                                          │
│ 위험 요인: [입력창]         [🔍 검색]    │
│                                          │
│ 위험성 수준: ○ 상  ○ 중  ○ 하           │
│                                          │
│ 개선 대책: [입력창]                      │
│                                          │
│ 작업 기간: [YYYY-MM-DD] ~ [YYYY-MM-DD]  │
└─────────────────────────────────────────┘
```

#### 위험도별 색상 코딩
- **상 (HIGH)**: `border-red-600 bg-red-50/50`
- **중 (MEDIUM)**: `border-amber-600 bg-amber-50/50`
- **하 (LOW)**: `border-green-600 bg-green-50/50`
- **미선택**: `border-gray-200 bg-white`

#### 삭제
- 카드 우측 상단 "항목 삭제" 버튼

### 4.5 UI 시각적 계층

배경색으로 계층 구분:
```
전체 배경 (white)
  └─ 대분류 카드 (bg-gray-50) ← 연한 회색
       └─ 소분류 섹션 (bg-orange-50/30 + ml-4) ← 연한 오렌지 + 들여쓰기
            └─ 위험요인 카드 (위험도별 색상)
```

### 4.6 유효성 검사

제출 시 검증 항목:

#### 1단계: 대분류 검사
- 최소 1개 이상의 대분류 필수
- 모든 대분류 선택 완료 확인

#### 2단계: 소분류 검사
- 각 대분류마다 최소 1개 이상의 소분류 필수
- alert: `대분류{index} ({categoryName})에 최소 1개 이상의 소분류를 선택해주세요.`

#### 3단계: 위험요인 검사
- 각 소분류마다 최소 1개 이상의 위험요인 필수
- alert: `소분류 "{subcategoryName}"에 최소 1개 이상의 위험요인을 추가해주세요.`

#### 4단계: 위험요인 상세 검사
각 위험요인의 필수 항목:
- **위험요인**: 공백 아님
- **위험성 수준**: 상/중/하 선택 필수
- **개선대책**: 공백 아님

## 5. 모달 컴포넌트

### 5.1 SubcategoryAddModal
**용도**: 소분류 직접 추가

**Props**:
```typescript
{
  isOpen: boolean;
  onClose: () => void;
  onAdd: (name: string) => void;
}
```

**기능**:
- 소분류 이름 입력
- 입력값 trim 처리
- 빈 문자열 체크

### 5.2 RiskFactorSelectModal
**용도**: 위험요인 선택/입력

**Props**:
```typescript
{
  isOpen: boolean;
  onClose: () => void;
  categoryId: number;
  subcategoryId: number;
  onSelect: (factors: { factor: string; improvement: string }[]) => void;
  initialMode?: 'search' | 'direct';
  isCustomSubcategory?: boolean;
  existingFactors?: string[];
}
```

**모드**:
1. **목록에서 선택** (`search`)
   - 일반 소분류: Mock 데이터 표시
   - 커스텀 소분류: 안내 메시지 + 직접 입력 유도
   - 이미 추가된 위험요인 제외

2. **직접 입력** (`direct`)
   - 위험요인 textarea
   - 개선대책 textarea
   - 중복 체크

**자동 모드 전환**:
- 커스텀 소분류 위험요인 추가 시 → 자동으로 `direct` 모드

### 5.3 SuccessModal
**용도**: 작성 완료 알림

**Props**:
```typescript
{
  isOpen: boolean;
  onConfirm: () => void;
  title?: string;
  message?: string;
}
```

**기능**:
- 성공 아이콘 표시
- 확인 버튼 클릭 시 `/safety/risk` 페이지로 이동

## 6. 디자인 시스템

### 6.1 색상 팔레트
- **Primary**: Orange (#F97316 → #EA580C)
- **위험도 상**: Red (#DC2626)
- **위험도 중**: Amber (#D97706)
- **위험도 하**: Green (#16A34A)
- **배경 계층**: Gray-50, Orange-50/30
- **텍스트**: Slate-800, Slate-600, Slate-500

### 6.2 타이포그래피
- **페이지 제목**: `text-xl font-black tracking-tight`
- **섹션 제목**: `text-lg font-bold`
- **레이블**: `text-sm font-medium`
- **버튼**: `font-medium` 또는 `font-bold`

### 6.3 버튼 스타일

#### Primary 버튼
```tsx
className="px-8 py-3 rounded-xl font-bold text-white
           bg-gradient-to-r from-orange-500 to-orange-600
           hover:from-orange-600 hover:to-orange-700"
```

#### Secondary 버튼
```tsx
className="px-6 py-2 rounded-lg font-medium text-slate-600
           bg-gray-100 hover:bg-gray-200"
```

#### Text 버튼
```tsx
className="text-sm text-orange-600 hover:text-orange-700 font-medium"
```

## 7. 상태 관리

### 7.1 로컬 상태
```typescript
// 기본 정보
const [siteName] = useState('통사통사현장');
const [companyName] = useState('(주)통하는사람들');
const [approvalLineId] = useState(1);
const [workerRepName] = useState('36');
const [workerRepId] = useState('0017');
const [workPeriodStart, setWorkPeriodStart] = useState('2026-01-01');
const [workPeriodEnd, setWorkPeriodEnd] = useState('2026-01-31');

// 작업 공종
const [categories, setCategories] = useState<Category[]>([]);

// 모달 상태
const [subcategoryModalOpen, setSubcategoryModalOpen] = useState(false);
const [riskFactorModalOpen, setRiskFactorModalOpen] = useState(false);
const [successModalOpen, setSuccessModalOpen] = useState(false);
const [activeCategory, setActiveCategory] = useState<string | null>(null);
const [activeSubcategory, setActiveSubcategory] = useState<number | null>(null);
```

### 7.2 주요 핸들러

#### 대분류 관리
- `handleAddCategory()` - 대분류 추가
- `handleCategoryChange()` - 대분류 선택
- `handleDeleteCategory()` - 대분류 삭제

#### 소분류 관리
- `handleSubcategoryToggle()` - 소분류 선택/해제
- `handleAddCustomSubcategory()` - 커스텀 소분류 추가

#### 위험요인 관리
- `handleAddRiskFactor()` - 위험요인 모달 열기
- `handleSelectRiskFactors()` - 위험요인 선택 완료
- `handleUpdateRiskFactor()` - 위험요인 필드 업데이트
- `handleDeleteRiskFactor()` - 위험요인 삭제

#### 폼 제출
- `handleSubmit()` - 유효성 검사 + API 호출 (TODO)

## 8. API 연동 (TODO)

### 8.1 필요한 API

#### 대분류 조회
```typescript
GET /api/risk-assessment/categories
Response: Category[]
```

#### 소분류 조회
```typescript
GET /api/risk-assessment/categories/:categoryId/subcategories
Response: Subcategory[]
```

#### 위험요인 추천 조회
```typescript
GET /api/risk-assessment/risk-factors/recommendations
Query: { categoryId, subcategoryId }
Response: RiskFactorOption[]
```

#### 위험성 평가 생성
```typescript
POST /api/risk-assessment/initial
Body: {
  siteName: string;
  companyName: string;
  approvalLineId: number;
  workPeriodStart: string;
  workPeriodEnd: string;
  categories: Category[];
}
Response: { id: number; ... }
```

## 9. 개선 예정 사항

### 9.1 단기 (우선순위 높음)
- [ ] 결재라인 선택 모달 구현
- [ ] API 연동 (Mock → Real DB)
- [ ] 작업기간 자동 복사 (전체 → 위험요인 개별)
- [ ] 로딩 상태 표시

### 9.2 중기
- [ ] Draft 저장 기능 (자동저장)
- [ ] 임시 저장 및 불러오기
- [ ] 이전 평가 복사 기능
- [ ] 엑셀 일괄 입력
- [ ] 위험요인 AI 추천 (GPT 연동)

### 9.3 장기
- [ ] 오프라인 지원 (IndexedDB)
- [ ] 위험성 평가 템플릿 저장
- [ ] 통계 및 분석 기능
- [ ] 다국어 지원

## 10. 테스트 시나리오

### 10.1 정상 플로우
1. 페이지 진입
2. 작업기간 선택
3. 대분류 추가 → 대분류 선택
4. 소분류 체크박스 선택
5. 위험요인 추가 (목록 선택)
6. 위험성 수준 선택 (상/중/하)
7. 개선대책 입력
8. "만들기" 버튼 클릭
9. 성공 모달 표시 → 목록 페이지로 이동

### 10.2 에지 케이스
- 대분류 없이 제출 → alert
- 소분류 없는 대분류 → alert
- 위험요인 없는 소분류 → alert
- 위험성 수준 미선택 → alert
- 중복 위험요인 추가 시도 → 목록에서 제외 또는 alert

### 10.3 UI/UX 테스트
- 대분류/소분류 배경색 구분 확인
- 위험도별 색상 표시 확인
- 소분류 순환 번호 표시 확인
- 버튼 왼쪽 정렬 확인
- 모달 중첩 form 에러 없음 확인

## 11. 참고 문서

- **기획서**: `docs/ui-specs/pc/plans/2026-01-16-최초위험성평가만들기-기획.md`
- **구현 명세**: `docs/ui-specs/pc/specs/2026-01-16-최초위험성평가만들기-구현.md`
- **디자인 가이드**: `docs/design_guideline_251221.md`
- **DB 스키마**: SQLite `safety_master.db` (추후 연동)

## 12. 변경 이력

### 2026-01-20
- 초기 구현 완료 (Mock 데이터)
- 페이지 제목 "최초 위험성 평가 만들기"로 변경
- 회사명에서 "시공사" 표시 제거
- 위험요인 추가 버튼 왼쪽 정렬
- 소분류 이름 표시 개선 (Mock 데이터 매핑)
- 위험요인 중복 선택 방지 기능 추가
- 커스텀 소분류 위험요인 직접입력 자동 전환
- UI 시각적 계층 구현 (배경색, 들여쓰기)
- 위험도별 색상 코딩 구현
- 폼 유효성 검사 강화
- 중첩 form 태그 에러 수정

---

**작성자**: Claude Code
**최종 수정일**: 2026-01-20
