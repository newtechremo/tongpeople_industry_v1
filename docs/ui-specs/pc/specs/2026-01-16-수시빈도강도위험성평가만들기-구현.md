# 수시 빈도강도 위험성평가 만들기 - 구현 명세서

> **원본**: `C:\hongtong\reference\figma\위험성평가 PC관리자\현장통 2.0 PC 관리자 -수시 빈도강도 만들기.mp4`
> **생성일**: 2026-01-16
> **플랫폼**: PC (admin-web)
> **문서 유형**: 구현 명세서 (2단계)
> **기획 문서**: `docs/ui-specs/pc/plans/2026-01-16-수시빈도강도위험성평가만들기-기획.md`

---

## 1. 컴포넌트 구조

### 1.1 컴포넌트 트리
```
AdHocAssessmentFormPage
├── Header
│   └── PageTitle ("수시 위험성평가 만들기")
├── AdHocAssessmentForm
│   ├── BasicInfoSection
│   │   ├── SiteInfoDisplay (현장명)
│   │   ├── CompanyInput (소속회사)
│   │   ├── ApprovalLineInput (결재라인)
│   │   │   └── ApprovalLineModal
│   │   ├── WorkerRepresentativeInput (근로자대표)
│   │   ├── DateRangePicker (작업기간)
│   │   │   └── DatePicker × 2
│   │   └── RiskLevelMethodSelector (위험성 수준 방식)
│   │       ├── Radio: 상·중·하
│   │       └── Radio: 빈도·강도 ← (선택됨)
│   ├── CategorySectionList
│   │   └── CategorySection (대분류①, ②, ...)
│   │       ├── CategorySearchInput (대분류 검색)
│   │       ├── SubcategoryCheckList (소분류 체크리스트)
│   │       └── SubcategoryRiskSection (소분류별 위험요인)
│   │           └── FrequencyIntensityRiskCard ← 핵심 신규 컴포넌트
│   │               ├── RiskFactorSearchInput (위험 요인)
│   │               ├── FrequencyIntensitySelector (빈도·강도)
│   │               │   ├── FrequencyRadioGroup (빈도 5단계)
│   │               │   ├── IntensityRadioGroup (강도 4단계)
│   │               │   └── RiskGradeDisplay (평가 등급 자동계산)
│   │               ├── ImprovementMeasureInput (개선 대책)
│   │               ├── AfterImprovementSection (개선 후 등급)
│   │               │   ├── FrequencyRadioGroup
│   │               │   ├── IntensityRadioGroup
│   │               │   └── RiskGradeDisplay
│   │               ├── ConfirmationSection (확인 여부)
│   │               │   ├── ConfirmationTypeRadio
│   │               │   └── ConfirmationTargetButton
│   │               │       └── ConfirmationTargetModal
│   │               └── ActionInfoSection (조치 정보)
│   │                   ├── DatePicker (조치일)
│   │                   ├── PersonSearchInput (조치자)
│   │                   │   └── PersonSearchModal
│   │                   ├── PersonSearchInput (조치확인자)
│   │                   │   └── PersonSearchModal
│   │                   └── ReviewContentInput (검토 내용)
│   ├── AddCategoryButton ("+ 작업 공종(대분류) 추가하기")
│   └── OptionalInfoSection (선택 정보 입력)
│       ├── UseToggleRadio (사용/미사용)
│       ├── TagInput (순회점검사항)
│       └── TagInput (아차사고)
└── FormActions
    ├── Button: 취소
    └── Button: 만들기
```

### 1.2 파일 구조
```
src/pages/risk-assessment/
├── AdHocAssessmentFormPage.tsx      # 메인 페이지
├── components/
│   ├── adhoc/
│   │   ├── AdHocAssessmentForm.tsx       # 폼 컨테이너
│   │   ├── BasicInfoSection.tsx          # 기본 정보 섹션
│   │   ├── CategorySection.tsx           # 대분류 섹션
│   │   ├── FrequencyIntensityRiskCard.tsx # 빈도·강도 위험요인 카드 ★
│   │   ├── FrequencyIntensitySelector.tsx # 빈도·강도 선택기 ★
│   │   ├── ConfirmationSection.tsx       # 확인 여부 섹션 ★
│   │   ├── ActionInfoSection.tsx         # 조치 정보 섹션 ★
│   │   └── OptionalInfoSection.tsx       # 선택 정보 섹션 ★
│   ├── modals/
│   │   ├── ConfirmationTargetModal.tsx   # 확인 대상 선택 모달 ★
│   │   ├── PersonSearchModal.tsx         # 결재자/조치자 검색 모달 ★
│   │   └── ApprovalLineModal.tsx         # 결재라인 모달 (기존 재사용)
│   └── shared/
│       ├── CategorySearchInput.tsx       # 대분류 검색 (기존 재사용)
│       ├── SubcategoryCheckList.tsx      # 소분류 체크리스트 (기존 재사용)
│       ├── RiskFactorSearchInput.tsx     # 위험요인 검색 (기존 재사용)
│       ├── TagInput.tsx                  # 태그 입력 ★
│       └── RiskGradeDisplay.tsx          # 등급 표시 ★
└── hooks/
    ├── useAdHocAssessment.ts             # 수시 평가 훅 ★
    ├── useFrequencyIntensity.ts          # 빈도·강도 계산 훅 ★
    └── usePersonSearch.ts                # 인원 검색 훅 ★
```

---

## 2. 컴포넌트 매핑

### 2.1 매핑 결과 요약
| 상태 | 개수 |
|------|------|
| ✅ 기존 사용 | 8개 |
| 🆕 신규 필요 | 14개 |
| ⚠️ 수정 필요 | 2개 |

### 2.2 상세 매핑

| UI 요소 | 상태 | 컴포넌트 | 경로 |
|---------|------|----------|------|
| 페이지 레이아웃 | ✅ | `Layout` | `src/components/layout/Layout.tsx` |
| 헤더 | ✅ | 직접 구현 | 페이지 내 |
| 입력 필드 | ✅ | `Input` | `src/components/common/Input.tsx` |
| 버튼 | ✅ | `Button` | `src/components/common/Button.tsx` |
| 날짜 선택 | ✅ | `DatePicker` | `src/components/common/DatePicker.tsx` |
| 모달 | ✅ | `Modal` | `src/components/common/Modal.tsx` |
| 대분류 검색 | ✅ | `CategorySearchInput` | `src/pages/risk-assessment/components/shared/` |
| 소분류 체크리스트 | ✅ | `SubcategoryCheckList` | `src/pages/risk-assessment/components/shared/` |
| 수시 평가 폼 | 🆕 | `AdHocAssessmentForm` | - |
| 빈도·강도 선택기 | 🆕 | `FrequencyIntensitySelector` | - |
| 빈도 라디오 그룹 | 🆕 | `FrequencyRadioGroup` | - |
| 강도 라디오 그룹 | 🆕 | `IntensityRadioGroup` | - |
| 위험 등급 표시 | 🆕 | `RiskGradeDisplay` | - |
| 빈도·강도 위험카드 | 🆕 | `FrequencyIntensityRiskCard` | - |
| 확인 여부 섹션 | 🆕 | `ConfirmationSection` | - |
| 확인 대상 모달 | 🆕 | `ConfirmationTargetModal` | - |
| 조치 정보 섹션 | 🆕 | `ActionInfoSection` | - |
| 인원 검색 모달 | 🆕 | `PersonSearchModal` | - |
| 선택 정보 섹션 | 🆕 | `OptionalInfoSection` | - |
| 태그 입력 | 🆕 | `TagInput` | - |
| 빈도·강도 계산 훅 | 🆕 | `useFrequencyIntensity` | - |
| 인원 검색 훅 | 🆕 | `usePersonSearch` | - |
| 위험성 수준 방식 선택 | ⚠️ | `RiskLevelMethodSelector` | 기존 확장 필요 |
| 위험성평가 라우팅 | ⚠️ | `RiskAssessmentPage` | 수시 평가 경로 추가 |

---

## 3. 신규 컴포넌트 명세

### 3.1 FrequencyIntensitySelector (빈도·강도 선택기)

**목적**: 빈도(5단계)와 강도(4단계)를 선택하고 평가 등급을 자동 계산하여 표시

**Props Interface**:
```typescript
interface FrequencyIntensitySelectorProps {
  // 빈도·강도 값
  frequency: FrequencyLevel | null;
  intensity: IntensityLevel | null;

  // 변경 핸들러
  onFrequencyChange: (value: FrequencyLevel) => void;
  onIntensityChange: (value: IntensityLevel) => void;

  // 옵션
  label?: string;           // 섹션 레이블 (예: "위험성 수준", "개선 후 등급")
  disabled?: boolean;
  showGrade?: boolean;      // 등급 표시 여부 (기본 true)
}

type FrequencyLevel = 1 | 2 | 3 | 4 | 5;
type IntensityLevel = 1 | 2 | 3 | 4;
```

**사용 예시**:
```tsx
<FrequencyIntensitySelector
  frequency={riskFactor.frequency_before}
  intensity={riskFactor.intensity_before}
  onFrequencyChange={(value) => handleFrequencyChange('before', value)}
  onIntensityChange={(value) => handleIntensityChange('before', value)}
  label="위험성 수준"
/>
```

**내부 구조**:
```tsx
// FrequencyIntensitySelector.tsx
export function FrequencyIntensitySelector({
  frequency,
  intensity,
  onFrequencyChange,
  onIntensityChange,
  label = "위험성 수준",
  disabled = false,
  showGrade = true,
}: FrequencyIntensitySelectorProps) {
  const grade = useMemo(() => {
    if (frequency && intensity) {
      return calculateRiskGrade(frequency, intensity);
    }
    return null;
  }, [frequency, intensity]);

  return (
    <div className="space-y-4">
      <h4 className="text-sm font-semibold text-slate-700">{label}</h4>

      {/* 빈도 라디오 그룹 */}
      <div className="flex items-center gap-4">
        <span className="w-12 text-sm text-slate-600">빈도</span>
        <div className="flex gap-2">
          {FREQUENCY_OPTIONS.map((opt) => (
            <label key={opt.value} className="flex items-center gap-1">
              <input
                type="radio"
                name="frequency"
                value={opt.value}
                checked={frequency === opt.value}
                onChange={() => onFrequencyChange(opt.value)}
                disabled={disabled}
                className="text-orange-500 focus:ring-orange-500"
              />
              <span className="text-sm">{opt.label}</span>
            </label>
          ))}
        </div>
      </div>

      {/* 강도 라디오 그룹 */}
      <div className="flex items-center gap-4">
        <span className="w-12 text-sm text-slate-600">강도</span>
        <div className="flex gap-2">
          {INTENSITY_OPTIONS.map((opt) => (
            <label key={opt.value} className="flex items-center gap-1">
              <input
                type="radio"
                name="intensity"
                value={opt.value}
                checked={intensity === opt.value}
                onChange={() => onIntensityChange(opt.value)}
                disabled={disabled}
                className="text-orange-500 focus:ring-orange-500"
              />
              <span className="text-sm">{opt.label}</span>
            </label>
          ))}
        </div>
      </div>

      {/* 평가 등급 표시 */}
      {showGrade && grade && (
        <div className="flex items-center gap-4">
          <span className="w-12 text-sm text-slate-600">평가 등급</span>
          <RiskGradeDisplay grade={grade} />
        </div>
      )}
    </div>
  );
}

// 상수 정의
const FREQUENCY_OPTIONS = [
  { value: 5, label: '최상(5)' },
  { value: 4, label: '상(4)' },
  { value: 3, label: '중(3)' },
  { value: 2, label: '하(2)' },
  { value: 1, label: '최하(1)' },
] as const;

const INTENSITY_OPTIONS = [
  { value: 4, label: '최대(4)' },
  { value: 3, label: '대(3)' },
  { value: 2, label: '중(2)' },
  { value: 1, label: '소(1)' },
] as const;
```

### 3.2 RiskGradeDisplay (위험 등급 표시)

**목적**: 계산된 위험 등급을 색상과 함께 표시

**Props Interface**:
```typescript
interface RiskGradeDisplayProps {
  grade: RiskGrade | null;
  size?: 'sm' | 'md' | 'lg';
}

type RiskGrade = '고등급 (긴급 조치)' | '중등급 (점검 관리)' | '하등급 (일상 관리)';
```

**사용 예시**:
```tsx
<RiskGradeDisplay grade="중등급 (점검 관리)" />
```

**스타일링**:
```tsx
const GRADE_STYLES = {
  '고등급 (긴급 조치)': 'bg-red-100 text-red-700 border-red-200',
  '중등급 (점검 관리)': 'bg-yellow-100 text-yellow-700 border-yellow-200',
  '하등급 (일상 관리)': 'bg-green-100 text-green-700 border-green-200',
};
```

### 3.3 ConfirmationSection (확인 여부 섹션)

**목적**: 근로자 확인 대상/비확인 선택 및 확인 대상 그룹 선택

**Props Interface**:
```typescript
interface ConfirmationSectionProps {
  confirmationType: 'WORKER_CONFIRM' | 'WORKER_NO_CONFIRM';
  confirmationTargets: ConfirmationTarget[];
  onTypeChange: (type: 'WORKER_CONFIRM' | 'WORKER_NO_CONFIRM') => void;
  onTargetsChange: (targets: ConfirmationTarget[]) => void;
}

type ConfirmationTarget = 'ALL' | 'WORKER' | 'SAFETY_TEAM';
```

**사용 예시**:
```tsx
<ConfirmationSection
  confirmationType={riskFactor.confirmation_type}
  confirmationTargets={riskFactor.confirmation_targets}
  onTypeChange={handleConfirmationTypeChange}
  onTargetsChange={handleConfirmationTargetsChange}
/>
```

### 3.4 ConfirmationTargetModal (확인 대상 선택 모달)

**목적**: 확인 대상 그룹(전체대상/근로자/안전팀) 체크박스 선택

**Props Interface**:
```typescript
interface ConfirmationTargetModalProps {
  isOpen: boolean;
  onClose: () => void;
  selectedTargets: ConfirmationTarget[];
  onConfirm: (targets: ConfirmationTarget[]) => void;
}
```

**사용 예시**:
```tsx
<ConfirmationTargetModal
  isOpen={isTargetModalOpen}
  onClose={() => setIsTargetModalOpen(false)}
  selectedTargets={selectedTargets}
  onConfirm={handleTargetsConfirm}
/>
```

### 3.5 PersonSearchModal (인원 검색 모달)

**목적**: 결재자, 조치자, 조치확인자 검색 및 선택

**Props Interface**:
```typescript
interface PersonSearchModalProps {
  isOpen: boolean;
  onClose: () => void;
  title: string;                    // "결재자 검색", "조치확인자 선택" 등
  onSelect: (person: Person) => void;

  // 필터 옵션 (결재자 검색용)
  showCompanyFilter?: boolean;      // 소속 회사 필터 표시
  showRoleFilter?: boolean;         // 검색 대상 필터 표시
}

interface Person {
  id: number;
  name: string;
  company: string;
  team: string;
  position: string;
}
```

**사용 예시**:
```tsx
<PersonSearchModal
  isOpen={isPersonModalOpen}
  onClose={() => setIsPersonModalOpen(false)}
  title="조치확인자 선택"
  onSelect={handlePersonSelect}
/>
```

### 3.6 ActionInfoSection (조치 정보 섹션)

**목적**: 조치일, 조치자, 조치확인자, 검토 내용 입력

**Props Interface**:
```typescript
interface ActionInfoSectionProps {
  actionDate: string;
  actionPerson: Person | null;
  actionVerifier: Person | null;
  reviewContents: string[];

  onActionDateChange: (date: string) => void;
  onActionPersonChange: (person: Person) => void;
  onActionVerifierChange: (person: Person) => void;
  onReviewContentAdd: (content: string) => void;
  onReviewContentRemove: (index: number) => void;
}
```

### 3.7 TagInput (태그 입력)

**목적**: 순회점검사항, 아차사고 등 태그 형식 입력

**Props Interface**:
```typescript
interface TagInputProps {
  label: string;
  placeholder?: string;
  tags: string[];
  onAddTag: (tag: string) => void;
  onRemoveTag: (index: number) => void;
  disabled?: boolean;
}
```

**사용 예시**:
```tsx
<TagInput
  label="순회점검사항"
  placeholder="순회점검사항을 입력하세요..."
  tags={patrolCheckItems}
  onAddTag={handleAddPatrolItem}
  onRemoveTag={handleRemovePatrolItem}
/>
```

### 3.8 OptionalInfoSection (선택 정보 입력 섹션)

**목적**: 순회점검사항, 아차사고 태그 입력 (사용/미사용 토글)

**Props Interface**:
```typescript
interface OptionalInfoSectionProps {
  enabled: boolean;
  patrolCheckItems: string[];
  nearMissItems: string[];

  onEnabledChange: (enabled: boolean) => void;
  onPatrolCheckItemsChange: (items: string[]) => void;
  onNearMissItemsChange: (items: string[]) => void;
}
```

---

## 4. 수정 필요 컴포넌트

### 4.1 RiskLevelMethodSelector

**현재 경로**: `src/pages/risk-assessment/components/shared/` (신규 생성 필요)

**수정 내용**:
- 위험성 수준 방식 선택 라디오 버튼 추가
- "상·중·하" / "빈도·강도" 두 가지 옵션

**Props Interface**:
```typescript
interface RiskLevelMethodSelectorProps {
  value: 'HIGH_MEDIUM_LOW' | 'FREQUENCY_INTENSITY';
  onChange: (method: 'HIGH_MEDIUM_LOW' | 'FREQUENCY_INTENSITY') => void;
  disabled?: boolean;
}
```

### 4.2 RiskAssessmentPage 라우팅 수정

**현재 경로**: `src/pages/RiskAssessmentPage.tsx`

**수정 내용**:
- 수시 위험성평가 폼 라우팅 추가
- 유형 선택 후 "빈도·강도" 방식 선택 시 `AdHocAssessmentFormPage`로 이동

```typescript
// 라우팅 추가
<Route path="/risk-assessment/adhoc/new" element={<AdHocAssessmentFormPage />} />
```

---

## 5. 스타일링 가이드

### 5.1 빈도·강도 라디오 버튼
```tsx
// 라디오 버튼 그룹
<div className="flex items-center gap-6">
  <span className="w-12 text-sm font-medium text-slate-600">빈도</span>
  <div className="flex gap-4">
    {options.map((opt) => (
      <label className="flex items-center gap-2 cursor-pointer">
        <input
          type="radio"
          className="w-4 h-4 text-orange-500 border-gray-300
                     focus:ring-orange-500 focus:ring-offset-0"
        />
        <span className="text-sm text-slate-700">{opt.label}</span>
      </label>
    ))}
  </div>
</div>
```

### 5.2 위험 등급 뱃지
```tsx
// 고등급 (긴급 조치)
className="px-3 py-1 text-sm font-medium rounded-full
           bg-red-100 text-red-700 border border-red-200"

// 중등급 (점검 관리)
className="px-3 py-1 text-sm font-medium rounded-full
           bg-yellow-100 text-yellow-700 border border-yellow-200"

// 하등급 (일상 관리)
className="px-3 py-1 text-sm font-medium rounded-full
           bg-green-100 text-green-700 border border-green-200"
```

### 5.3 태그 입력 스타일
```tsx
// 태그 컨테이너
<div className="flex flex-wrap gap-2">
  {tags.map((tag, index) => (
    <span
      key={index}
      className="inline-flex items-center gap-1 px-3 py-1
                 bg-slate-100 text-slate-700 rounded-full text-sm"
    >
      -{tag}
      <button
        onClick={() => onRemove(index)}
        className="ml-1 text-slate-400 hover:text-red-500"
      >
        <X className="w-3 h-3" />
      </button>
    </span>
  ))}
</div>

// 입력 필드 + 추가 버튼
<div className="flex gap-2">
  <input
    className="flex-1 px-4 py-2 border border-gray-200 rounded-lg
               focus:outline-none focus:ring-2 focus:ring-orange-500"
  />
  <button
    className="px-4 py-2 bg-orange-500 text-white rounded-lg
               hover:bg-orange-600 font-medium"
  >
    추가하기
  </button>
</div>
```

### 5.4 확인 여부 라디오
```tsx
<div className="flex items-center gap-6">
  <span className="text-sm font-medium text-slate-600">확인 여부</span>
  <div className="flex gap-4">
    <label className="flex items-center gap-2 cursor-pointer">
      <input
        type="radio"
        checked={confirmationType === 'WORKER_CONFIRM'}
        className="w-4 h-4 text-orange-500"
      />
      <span className="text-sm">근로자 확인 대상</span>
    </label>
    <label className="flex items-center gap-2 cursor-pointer">
      <input
        type="radio"
        checked={confirmationType === 'WORKER_NO_CONFIRM'}
        className="w-4 h-4 text-orange-500"
      />
      <span className="text-sm">근로자 비확인</span>
    </label>
  </div>
</div>
```

### 5.5 색상 참조
| 용도 | Tailwind 클래스 | HEX |
|------|-----------------|-----|
| Primary | `orange-500` | `#F97316` |
| Primary Hover | `orange-600` | `#EA580C` |
| 고등급 배경 | `red-100` | `#FEE2E2` |
| 고등급 텍스트 | `red-700` | `#B91C1C` |
| 중등급 배경 | `yellow-100` | `#FEF3C7` |
| 중등급 텍스트 | `yellow-700` | `#A16207` |
| 하등급 배경 | `green-100` | `#DCFCE7` |
| 하등급 텍스트 | `green-700` | `#15803D` |

---

## 6. 상태 관리

### 6.1 서버 상태 (React Query)

```typescript
// 수시 위험성평가 생성
const createAdHocMutation = useMutation({
  mutationFn: (data: CreateAdHocAssessmentRequest) =>
    api.post('/risk-assessments/adhoc', data),
  onSuccess: () => {
    queryClient.invalidateQueries({ queryKey: ['risk-assessments'] });
    toast.success('수시 위험성평가가 생성되었습니다.');
    navigate('/risk-assessment');
  },
  onError: (error) => {
    toast.error('저장에 실패했습니다. 다시 시도해주세요.');
  },
});

// 인원 검색
const { data: persons, isLoading } = useQuery({
  queryKey: ['persons', { company, role, search }],
  queryFn: () => api.get('/sites/:siteId/users', {
    params: { company, role, search }
  }),
  enabled: isModalOpen,
});
```

### 6.2 폼 상태 (React Hook Form)

```typescript
const form = useForm<AdHocAssessmentFormData>({
  resolver: zodResolver(adHocAssessmentSchema),
  defaultValues: {
    risk_level_method: 'FREQUENCY_INTENSITY',
    categories: [],
    optional_info_enabled: false,
    patrol_check_items: [],
    near_miss_items: [],
  },
});

// 스키마
const adHocAssessmentSchema = z.object({
  company_name: z.string().min(1, '소속회사를 입력해주세요'),
  work_period_start: z.string().min(1, '시작일을 선택해주세요'),
  work_period_end: z.string().min(1, '종료일을 선택해주세요'),
  risk_level_method: z.enum(['HIGH_MEDIUM_LOW', 'FREQUENCY_INTENSITY']),
  categories: z.array(categorySchema).min(1, '최소 1개의 작업 공종을 선택해주세요'),
  // ...
});
```

### 6.3 로컬 상태

```typescript
// 모달 상태
const [isPersonModalOpen, setIsPersonModalOpen] = useState(false);
const [isTargetModalOpen, setIsTargetModalOpen] = useState(false);
const [modalContext, setModalContext] = useState<'approver' | 'actionPerson' | 'verifier'>('approver');

// 선택 상태
const [selectedCategoryIndex, setSelectedCategoryIndex] = useState<number | null>(null);
const [selectedSubcategoryIndex, setSelectedSubcategoryIndex] = useState<number | null>(null);
```

---

## 7. 유틸리티 함수

### 7.1 빈도·강도 등급 계산

```typescript
// utils/riskGrade.ts
export type FrequencyLevel = 1 | 2 | 3 | 4 | 5;
export type IntensityLevel = 1 | 2 | 3 | 4;
export type RiskGrade = '고등급 (긴급 조치)' | '중등급 (점검 관리)' | '하등급 (일상 관리)';

export function calculateRiskScore(
  frequency: FrequencyLevel,
  intensity: IntensityLevel
): number {
  return frequency * intensity;
}

export function calculateRiskGrade(
  frequency: FrequencyLevel,
  intensity: IntensityLevel
): RiskGrade {
  const score = calculateRiskScore(frequency, intensity);

  if (score >= 16) return '고등급 (긴급 조치)';
  if (score >= 8) return '중등급 (점검 관리)';
  return '하등급 (일상 관리)';
}

export function getRiskGradeColor(grade: RiskGrade): string {
  switch (grade) {
    case '고등급 (긴급 조치)':
      return 'bg-red-100 text-red-700 border-red-200';
    case '중등급 (점검 관리)':
      return 'bg-yellow-100 text-yellow-700 border-yellow-200';
    case '하등급 (일상 관리)':
      return 'bg-green-100 text-green-700 border-green-200';
  }
}
```

### 7.2 빈도·강도 레이블

```typescript
// constants/riskLevel.ts
export const FREQUENCY_LABELS: Record<FrequencyLevel, string> = {
  5: '최상',
  4: '상',
  3: '중',
  2: '하',
  1: '최하',
};

export const INTENSITY_LABELS: Record<IntensityLevel, string> = {
  4: '최대',
  3: '대',
  2: '중',
  1: '소',
};

export const FREQUENCY_OPTIONS = [
  { value: 5 as const, label: '최상(5)' },
  { value: 4 as const, label: '상(4)' },
  { value: 3 as const, label: '중(3)' },
  { value: 2 as const, label: '하(2)' },
  { value: 1 as const, label: '최하(1)' },
];

export const INTENSITY_OPTIONS = [
  { value: 4 as const, label: '최대(4)' },
  { value: 3 as const, label: '대(3)' },
  { value: 2 as const, label: '중(2)' },
  { value: 1 as const, label: '소(1)' },
];
```

---

## 8. 디자인 시스템 참조

**참조 문서**: `docs/design_guideline_251221.md`

| 항목 | 섹션 |
|------|------|
| 색상 | Primary Colors |
| 타이포그래피 | Typography Scale |
| 버튼 | Button Variants |
| 라디오 버튼 | Form Elements |
| 뱃지/태그 | Badge Styles |

---

## 9. 구현 체크리스트

### 9.1 컴포넌트
- [ ] `AdHocAssessmentFormPage.tsx` - 메인 페이지
- [ ] `AdHocAssessmentForm.tsx` - 폼 컨테이너
- [ ] `FrequencyIntensitySelector.tsx` - 빈도·강도 선택기
- [ ] `RiskGradeDisplay.tsx` - 등급 표시 컴포넌트
- [ ] `FrequencyIntensityRiskCard.tsx` - 위험요인 카드
- [ ] `ConfirmationSection.tsx` - 확인 여부 섹션
- [ ] `ConfirmationTargetModal.tsx` - 확인 대상 모달
- [ ] `ActionInfoSection.tsx` - 조치 정보 섹션
- [ ] `PersonSearchModal.tsx` - 인원 검색 모달
- [ ] `OptionalInfoSection.tsx` - 선택 정보 섹션
- [ ] `TagInput.tsx` - 태그 입력 컴포넌트

### 9.2 훅
- [ ] `useAdHocAssessment.ts` - 수시 평가 CRUD 훅
- [ ] `useFrequencyIntensity.ts` - 빈도·강도 계산 훅
- [ ] `usePersonSearch.ts` - 인원 검색 훅

### 9.3 유틸리티
- [ ] `utils/riskGrade.ts` - 등급 계산 유틸
- [ ] `constants/riskLevel.ts` - 빈도·강도 상수

### 9.4 라우팅 및 연동
- [ ] 라우트 등록 (`src/App.tsx`)
- [ ] 사이드바 메뉴 연동
- [ ] API 함수 생성 (`src/api/riskAssessment.ts`)

### 9.5 검증
- [ ] 폼 유효성 검사 (Zod 스키마)
- [ ] 로딩/에러/빈 상태 처리
- [ ] 반응형 레이아웃 확인

---

## 10. 연결 문서

- **기획 문서**: `docs/ui-specs/pc/plans/2026-01-16-수시빈도강도위험성평가만들기-기획.md`
- **디자인 가이드**: `docs/design_guideline_251221.md`
- **최초 위험성평가 명세**: `docs/ui-specs/pc/specs/2026-01-16-최초위험성평가만들기-구현.md`
